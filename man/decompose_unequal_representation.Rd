% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose-unequal-representation.R
\name{decompose_unequal_representation}
\alias{decompose_unequal_representation}
\title{Decompose unequal representation}
\usage{
decompose_unequal_representation(
  data,
  party_var,
  district_var,
  votes_var,
  elected_var,
  alpha = 2,
  group_decomposition = "none",
  independent_label = "Independent",
  election_info = FALSE
)
}
\arguments{
\item{data}{A data frame with one row per candidate.}

\item{party_var, district_var, votes_var, elected_var}{Unquoted column names
identifying the party, district, votes, and election outcome variables.}

\item{alpha}{Numeric scalar controlling the alpha-divergence.
\code{alpha = 0} and \code{alpha = 1} use their limiting forms.}

\item{group_decomposition}{Character string selecting optional decomposition
of party-level disproportionality. One of \code{"none"},
\code{"party_vs_independent"}, or \code{"multicandidate_vs_others"}.}

\item{independent_label}{Character scalar used to identify independents in \code{party_var}.
When \code{group_decomposition = "party_vs_independent"},
matching candidates are renumbered internally so
each independent is treated as a separate party.}

\item{election_info}{Logical scalar indicating whether election-level
metadata should be added to the returned tibble.}
}
\value{
A one-row tibble. It always includes \code{group_decomposition},
\code{whole_picture_of_unequal_representation}, \code{disproportionality},
\code{intra_party_unequal_representation}, \code{malapportionment}, and
\code{wasted_votes}. Additional columns are added when grouped decomposition
and/or \code{election_info = TRUE} are requested.
}
\description{
\code{decompose_unequal_representation()} aggregates candidate-level election results
to party-by-district totals and decomposes alpha-divergence between
seat shares and vote shares into disproportionality, malapportionment,
wasted votes, and intra-party unequal representation.
}
\details{
Districts where \code{votes_var} is entirely missing are treated
as no-contest districts and excluded from the decomposition.
Within a district, \code{votes_var} must be either fully observed or fully missing.
}
\examples{
election <- tibble::tribble(
    ~district, ~party, ~votes, ~elected,
    "D1", "Party A", 60, 1,
    "D1", "Party B", 40, 1,
    "D2", "Party A", 40, 1,
    "D2", "Party B", 60, 1
)

decompose_unequal_representation(
    data = election,
    party_var = party,
    district_var = district,
    votes_var = votes,
    elected_var = elected,
    alpha = 2,
    election_info = TRUE
)

}
